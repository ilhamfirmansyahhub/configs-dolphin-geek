# Dolphin Geek Configuration

Backup configuration for Dolphin on the `geek` user.

## Restore

Copy the backed-up files to the matching paths under `~/.config/`, then restart Dolphin.

> Note: this repository currently contains the configuration files captured from the `geek` setup. Files that contain machine-specific paths or session-specific state should be reviewed before restoring on a fresh OS.